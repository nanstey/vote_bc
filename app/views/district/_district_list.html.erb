<h5><%= year %> Districts <%= link_to "Top", "#top", class: 'button' %></h5>
<table>
  <thead>
    <tr>
      <th>District Name</th>
      <th>Abbreviation</th>
      <% if year != Election.current_election_year %>
        <th>Winner</th>
      <% end %>
    </tr>
  </thead>
  <tbody>
    <% election_districts.each do |ed| %>
      <tr>
        <td><%= link_to ed.district.name, "/district/#{ed.district.slug}/#{year}" %></td>
        <td><%= ed.district.abbr %></td>
        <% if year != Election.current_election_year %>
          <td><i class="fa fa-square" aria-hidden="true" style="color:<%= ed.winner.party.color %>"></i> <%= ed. winner.name %><td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>